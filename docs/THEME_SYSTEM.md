# NeoView 主题系统

## 概述

NeoView 现已集成 [tweakcn.com](https://tweakcn.com/editor/theme) 主题系统,支持自由更换和自定义主题。

## 当前主题

✨ **Amethyst Haze** - 优雅的紫色调主题已应用

## 功能特性

### 1. 预设主题
- 🎨 内置多个精选主题
- 🚀 一键快速切换
- 💾 自动保存配置

### 2. 自定义主题
- 🔗 支持从 URL 导入主题
- 📝 支持本地 JSON 文件导入
- 🎯 完全兼容 tweakcn 格式

### 3. 在线编辑器
- 🌐 集成 tweakcn.com 编辑器
- 🎨 可视化颜色调整
- 📤 一键导出和应用

## 使用方法

### 方式一:使用预设主题

1. 打开设置面板
2. 进入"主题管理"标签
3. 在预设主题列表中选择一个主题
4. 点击应用
5. 刷新页面查看效果

### 方式二:使用 tweakcn 编辑器

1. 访问 [tweakcn.com/editor/theme](https://tweakcn.com/editor/theme)
2. 在编辑器中自定义颜色:
   - Primary: 主色调
   - Secondary: 次要色
   - Accent: 强调色
   - Background: 背景色
   - 等等...
3. 点击右上角的"分享"按钮
4. 复制生成的主题 URL
5. 在 NeoView 的主题管理面板中粘贴 URL
6. 点击"应用"
7. 刷新页面

### 方式三:手动编辑 CSS

直接编辑 `src/app.css` 文件中的 CSS 变量:

```css
:root {
  --radius: 0.5rem;
  --background: oklch(0.9777 0.0041 301.4256);
  --foreground: oklch(0.3651 0.0325 287.0807);
  --primary: oklch(0.6104 0.0767 299.7335);
  /* ... 更多变量 */
}

.dark {
  --background: oklch(0.2166 0.0215 292.8474);
  --foreground: oklch(0.9053 0.0245 293.5570);
  /* ... 暗色模式变量 */
}
```

## 可用的颜色变量

### 基础颜色
- `--background`: 背景色
- `--foreground`: 前景色(文字)
- `--card`: 卡片背景
- `--card-foreground`: 卡片文字

### 语义颜色
- `--primary`: 主色调
- `--primary-foreground`: 主色调文字
- `--secondary`: 次要色
- `--secondary-foreground`: 次要色文字
- `--accent`: 强调色
- `--accent-foreground`: 强调色文字
- `--muted`: 柔和色
- `--muted-foreground`: 柔和色文字
- `--destructive`: 破坏性操作色(删除、警告等)

### 边框和输入
- `--border`: 边框色
- `--input`: 输入框边框色
- `--ring`: 焦点环颜色

### 侧边栏
- `--sidebar`: 侧边栏背景
- `--sidebar-foreground`: 侧边栏文字
- `--sidebar-primary`: 侧边栏主色
- `--sidebar-accent`: 侧边栏强调色
- `--sidebar-border`: 侧边栏边框

### 其他
- `--radius`: 圆角半径

## 颜色格式

使用 OKLCH 颜色格式:
```css
oklch(亮度 色度 色相)
oklch(0.5 0.1 180)

/* 带透明度 */
oklch(0.5 0.1 180 / 50%)
```

参数说明:
- **亮度 (L)**: 0-1,0 为黑色,1 为白色
- **色度 (C)**: 0-0.4,表示颜色饱和度
- **色相 (H)**: 0-360,色轮角度
- **透明度 (A)**: 0-100%,可选

## 预设主题列表

1. **Amethyst Haze** (当前)
   - 优雅的紫色调
   - URL: `https://tweakcn.com/r/themes/amethyst-haze.json`

2. **Ocean Breeze**
   - 清新的海洋蓝
   - URL: `https://tweakcn.com/r/themes/ocean-breeze.json`

3. **Forest Mist**
   - 自然的森林绿
   - URL: `https://tweakcn.com/r/themes/forest-mist.json`

4. **Sunset Glow**
   - 温暖的日落橙
   - URL: `https://tweakcn.com/r/themes/sunset-glow.json`

## 开发者指南

### 添加新的预设主题

编辑 `src/lib/utils/themeManager.ts`:

```typescript
export const PRESET_THEMES = [
  {
    name: '你的主题名称',
    url: 'https://tweakcn.com/r/themes/your-theme.json',
    description: '主题描述'
  },
  // ... 其他主题
];
```

### 程序化应用主题

```typescript
import { fetchThemeFromURL, applyThemeToCSS } from '$lib/utils/themeManager';

// 从 URL 获取并应用主题
const theme = await fetchThemeFromURL('https://tweakcn.com/r/themes/your-theme.json');
await applyThemeToCSS(theme, 'src/app.css');
```

## 故障排除

### 主题没有生效?
1. 确保已刷新页面
2. 检查浏览器控制台是否有错误
3. 验证 CSS 变量是否正确应用

### 颜色显示不正确?
1. 确保使用 OKLCH 格式
2. 检查亮度值是否在 0-1 范围内
3. 验证色度值是否合理(通常 0-0.4)

### 无法从 URL 导入主题?
1. 检查网络连接
2. 确保 URL 格式正确
3. 验证 URL 返回的是有效的 JSON

## 相关链接

- [tweakcn.com 主题编辑器](https://tweakcn.com/editor/theme)
- [shadcn/ui 主题文档](https://ui.shadcn.com/themes)
- [OKLCH 颜色格式](https://oklch.com/)
- [Tailwind CSS v4 文档](https://tailwindcss.com/docs)

## 更新日志

### 2025-11-21
- ✅ 集成 tweakcn.com 主题系统
- ✅ 应用 Amethyst Haze 主题
- ✅ 创建主题管理面板
- ✅ 支持预设主题和自定义主题
- ✅ 添加主题工具类
